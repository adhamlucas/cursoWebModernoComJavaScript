console.log(soma(3, 4)) // quando faz function declaration, o js primeiro delcara essas funcoess

// functoin declaration
function soma(x, y) {
    return x + y
}

// function expression
const sub = function(x, y){
    return x - y
}

// named function xpressed
const mult = function mult(x, y) {
    return x * y
}